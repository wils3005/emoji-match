{
  "name": "emoji-match",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "rimraf build/ && tsc",
    "build": "esbuild --bundle --minify --outfile=build/emoji-match.js src/index.ts",
    "postbuild": "cp build/emoji-match.js docs/",
    "check": "npm-check --save-exact --update"
  },
  "dependencies": {
    "phaser": "^3.55.2",
    "zod": "^3.11.6"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.6.0",
    "@typescript-eslint/parser": "^5.6.0",
    "esbuild": "^0.14.2",
    "eslint": "^8.4.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.25.3",
    "eslint-plugin-prettier": "^4.0.0",
    "npm-check": "^5.9.2",
    "prettier": "^2.5.1",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.2"
  }
}
